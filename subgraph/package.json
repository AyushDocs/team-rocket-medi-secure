{
  "name": "sanjeevni-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for Sanjeevni Healthcare Platform",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 sanjeevni/local",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.28.0",
    "assemblyscript": "^0.19.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
